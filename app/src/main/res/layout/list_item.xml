<?xml version="1.0" encoding="utf-8"?>

<layout>
    <data>
        <variable
            name="itemViewModel"
            type="com.example.atuski.qiitaqlient.viewmodel.ItemViewModel" />

    </data>


    <RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="10dp">

        <ImageView
            android:id="@+id/image_view"
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:layout_marginRight="10dp"
            android:src="@mipmap/ic_launcher"/>

        <TextView android:id="@+id/article_title_view"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentTop="true"
            android:layout_toRightOf="@id/image_view"
            android:ellipsize="end"
            android:lines="1"
            android:textSize="20sp"
            android:text="@{itemViewModel.repo.title}"
            tools:text="track"/>

        <TextView android:id="@+id/article_url_view"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:layout_toRightOf="@id/image_view"
            android:ellipsize="end"
            android:lines="1"
            android:text="@{itemViewModel.repo.url}"
            tools:text="artist"/>

    </RelativeLayout>

</layout>
