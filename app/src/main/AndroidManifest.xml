<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.atuski.qiitaqlient">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".QiitaQlientApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_2"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_2_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data android:name="awsAccessKey" android:value="${awsAccessKey}"/>
        <meta-data android:name="awsSecretKey" android:value="${awsSecretKey}"/>

        <!--<meta-data android:name="firebase_messaging_auto_init_enabled" android:value="false" />-->

        <service android:name=".service.NotificationMessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT"/>
            </intent-filter>
        </service>

        <service
            android:name=".service.NotificationInstanceIDService">
            <intent-filter>
                <action android:name="com.google.firebase.INSTANCE_ID_EVENT"/>
            </intent-filter>
        </service>

        <activity android:name=".MainActivity"
            android:noHistory="false">
            <!--android:noHistory="false"-->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="lightqiita.com" />

            </intent-filter>
        </activity>

        <activity android:name=".PushReceiveActivity"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="PUSH" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.detail.DetailActivity"
            android:label="@string/app_name2">
        </activity>
        <activity
            android:name=".ui.searchhistory.SearchHistoryActivity"
            android:label="@string/searchHistory">
        </activity>
    </application>

</manifest>